export enum Sorting {
  ASC = 'asc',
  DESC = 'desc',
}

export enum InterviewStatus {
  TODO = 'todo',
  IN_PROGRESS = 'inprogress',
  DONE = 'done',
}

export enum CommentStatus {
  PUBLISH = 'publish',
  DELETED = 'deleted',
}
