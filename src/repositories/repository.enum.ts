export enum RepositoryProvider {
  ACCOUNT = 'account-repository-provider',
  INTERVIEW = 'interview-repository-provider',
  COMMENT = 'comment-repository-provider',
  LOG = 'log-repository-provider',
}
